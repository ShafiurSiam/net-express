# গ্রাহকদের মতামত (Reviews) — অতিরিক্ত গাইড

এই গাইডটি `MAINTENANCE-GUIDE.md`-এর সাথে পড়ুন। এখানে বলা আছে ওয়েবসাইটের "গ্রাহকদের মতামত" সেকশন কীভাবে কাজ করে, শুরুর রিভিউ কীভাবে যোগ করবেন, কোনো অনুপযুক্ত রিভিউ কীভাবে লুকাবেন এবং Google Apps Script কীভাবে চালু করবেন।

## ১. সেকশনটি কীভাবে কাজ করে

- সেকশনটি হোমপেজে FAQ-এর নিচে, "সংযোগের জন্য আবেদন করুন" অংশের ঠিক আগে দেখা যায়।
- প্রতিটি রিভিউ কার্ডে শুধু **তারা (১–৫), নাম ও মন্তব্য** দেখানো হয় — কোনো ছবি বা অক্ষরের বৃত্ত নেই।
- উপরে সব দৃশ্যমান রিভিউ থেকে **গড় রেটিং** ও **মোট রিভিউ সংখ্যা** হিসাব করে দেখানো হয়।
- প্রথমে ৬টি রিভিউ দেখানো হয়; বাকিগুলো "আরও দেখুন" বাটনে।
- ক্রম: সদ্য জমা দেওয়া রিভিউ সবার আগে, তারপর Google Sheet-এর রিভিউ (নতুন থেকে পুরনো), সবশেষে `src/data/reviews.js`-এর শুরুর রিভিউ।
- ভিজিটররা "আপনার মতামত লিখুন" বাটনে চাপ দিয়ে রিভিউ দেন। জমা দেওয়ার সাথে সাথেই তা সাইটে প্রকাশিত হয় (আগে থেকে অনুমোদনের কোনো ধাপ নেই)।
- **সেকশনটি কখন লুকিয়ে থাকে:** যদি `VITE_REVIEWS_WEBHOOK_URL` সেট করা না থাকে **এবং** `src/data/reviews.js`-এ কোনো সম্পূর্ণ রিভিউ না থাকে, তাহলে পুরো সেকশনটি সাইটে দেখা যায় না। `VITE_REVIEWS_WEBHOOK_URL` না থাকলে "আপনার মতামত লিখুন" বাটনও থাকে না।

## ২. শুরুর রিভিউ যোগ করা (`src/data/reviews.js`)

ফাইলটি: **`src/data/reviews.js`**

ফাইলে ১০টি ফাঁকা জায়গা (placeholder) আছে। **শুধু সত্যিকারের গ্রাহকের মতামত, গ্রাহকের অনুমতি নিয়ে** বসান। নিজে থেকে বানানো নাম বা মন্তব্য কখনো বসাবেন না।

একটি এন্ট্রি এমন:

```js
{ id: "starter-1", name: "গ্রাহকের নাম", rating: 5, comment: { bn: "বাংলা মন্তব্য", en: "" } },
```

- `name` — গ্রাহকের নাম।
- `rating` — ১ থেকে ৫ এর মধ্যে একটি পূর্ণ সংখ্যা।
- `comment` — `bn` (বাংলা) ও `en` (ইংরেজি)। **একটি ভাষায় লিখলেই চলে**, অন্যটি ফাঁকা রাখলে সাইট নিজে থেকে যেটা আছে সেটা দেখাবে।
- `id` — প্রতিটির জন্য আলাদা হতে হবে। নতুন এন্ট্রি যোগ করলে `starter-11`, `starter-12` এভাবে দিন।
- **নাম ফাঁকা, মন্তব্য ফাঁকা বা রেটিং ১–৫ এর বাইরে (যেমন ০) থাকলে এন্ট্রিটি নিজে থেকেই লুকানো থাকে।** তাই ফাঁকা জায়গাগুলো রেখে দিলে কোনো ক্ষতি নেই।

সেভ করে GitHub-এ পাঠালে (push) লাইভ সাইটে পরিবর্তন আসবে।

## ৩. অনুপযুক্ত রিভিউ লুকানো (Google Sheet)

ওয়েবসাইট থেকে জমা হওয়া সব রিভিউ Google Sheet-এর **"Reviews"** ট্যাবে জমা হয়।

1. রিভিউ-এর Google Sheet-টি Google Drive থেকে খুলুন।
2. "Reviews" ট্যাবে সংশ্লিষ্ট রিভিউয়ের সারি খুঁজুন।
3. সেই সারির **Visible** কলামের ঘরটি **FALSE** করুন (টিকচিহ্ন থাকলে তা তুলে দিন, বা `FALSE` লিখুন)।
4. কিছুক্ষণের মধ্যে (পেজ রিফ্রেশ করলে) রিভিউটি সাইট থেকে সরে যাবে; গড় রেটিং ও সংখ্যাও নিজে থেকে বদলে যাবে।

আবার দেখাতে চাইলে Visible আবার `TRUE` করুন।

> কলামগুলো (Timestamp, Name, Rating, Comment, Lang, Visible) মুছবেন না, ক্রম বদলাবেন না এবং সারিগুলো সাজানোর (sort) দরকার নেই।

## ৪. Google Apps Script চালু করা (Web App হিসেবে Deploy)

স্ক্রিপ্টের কোড আছে **`docs/reviews-apps-script.gs`** ফাইলে। এটি সংযোগের আবেদন ফর্মের স্ক্রিপ্ট থেকে **আলাদা**, তাই নতুন Sheet ও নতুন স্ক্রিপ্ট লাগবে।

1. Google Sheets-এ একটি **নতুন Sheet** বানান (নাম যেকোনো, যেমন "Net Express Reviews")।
2. মেনু থেকে **Extensions → Apps Script** খুলুন।
3. সেখানকার পুরনো কোড মুছে `docs/reviews-apps-script.gs`-এর সব কোড কপি করে বসান এবং সেভ করুন।
4. উপরে ডানে **Deploy → New deployment** চাপুন। "Select type" থেকে গিয়ার আইকন চেপে **Web app** বেছে নিন।
5. নিচের দুটি সেটিং **অবশ্যই** এভাবে দিন:
   - **Execute as: Me** (আপনার নিজের অ্যাকাউন্ট)
   - **Who has access: Anyone**

   এর একটিও ভুল হলে ওয়েবসাইট স্ক্রিপ্টের উত্তর পড়তে পারবে না।
6. **Deploy** চাপুন এবং প্রথমবার Google যে অনুমতি চাইবে তা দিন (Advanced → Go to project → Allow)।
7. যে **Web app URL** (`https://script.google.com/macros/s/.../exec`) পাবেন সেটি কপি করুন।
8. সেই URL `VITE_REVIEWS_WEBHOOK_URL`-এর মান হিসেবে বসান:
   - নিজের কম্পিউটারের `.env` ফাইলে;
   - **লাইভ সার্ভারের `.env` ফাইলে** (`/var/www/net-express/.env`)। এটি না করলে লাইভ সাইটে রিভিউ ফর্ম আসবে না।
9. `VITE_...` মানগুলো সাইট **build করার সময়** ঢুকে যায়, তাই সার্ভারের `.env` বদলানোর পর নতুন করে build লাগবে: GitHub-এ `main`-এ যেকোনো একটি নতুন commit push করলে (যেমন এই বদলের commit) স্বয়ংক্রিয় deploy চলবে এবং নতুন মান ঢুকে যাবে। শুধু `.env` বদলে build না করলে সাইটে পরিবর্তন আসবে না।

### গুরুত্বপূর্ণ: লাইভ সাইট থেকে একটি পরীক্ষা করুন

Deploy করার পর **লাইভ সাইট থেকে নিজে একটি পরীক্ষামূলক রিভিউ জমা দিন** এবং নিশ্চিত করুন যে ফর্মে **"ধন্যবাদ! আপনার মতামত প্রকাশিত হয়েছে।"** বার্তাটি দেখা যাচ্ছে। এই বার্তা দেখা গেলেই প্রমাণ হয় যে স্ক্রিপ্টের `{ok:true}` উত্তর সাইট সফলভাবে পড়তে পারছে (অর্থাৎ Deploy সেটিং ঠিক আছে)। বার্তার বদলে ত্রুটি দেখালে ধাপ ৫-এর দুটি সেটিং আবার যাচাই করুন। এরপর Sheet-এ সারিটি এসেছে কি না দেখুন এবং পরীক্ষার রিভিউটি Visible = FALSE করে দিন।

### স্ক্রিপ্ট বদলালে

কোড বদলালে **Deploy → Manage deployments → (কলমের আইকন) → Version: New version → Deploy** করুন। এতে URL একই থাকে। "New deployment" করলে URL বদলে যায় এবং `.env` আবার বদলাতে হয়।

## ৫. স্পাম থেকে সুরক্ষা

- ওয়েবসাইটে: লুকানো একটি ফাঁদ-ঘর (honeypot) এবং প্রতি ব্রাউজারে **১০ মিনিটে ১টি** রিভিউ-এর সীমা। এ দুটি শুধু ব্রাউজারে কাজ করে, তাই সরাসরি URL-এ পাঠালে এড়ানো সম্ভব।
- স্ক্রিপ্টে (সার্ভার-সাইড): নাম ১–৫০ অক্ষর, মন্তব্য ১০–৫০০ অক্ষর ও রেটিং ১–৫ যাচাই; একই নাম + একই মন্তব্য দ্বিতীয়বার গ্রহণ করা হয় না; এবং **ঘণ্টায় সর্বোচ্চ ২০টি** রিভিউ (সবার মিলিয়ে) নেওয়া হয়। এই সংখ্যা বদলাতে স্ক্রিপ্টের উপরের দিকের `MAX_SUBMISSIONS_PER_HOUR` বদলান, তারপর উপরের "স্ক্রিপ্ট বদলালে" ধাপ অনুসরণ করুন।
- `=`, `+`, `-`, `@` দিয়ে শুরু হওয়া লেখা Sheet-এ ফর্মুলা হিসেবে চলতে না পারে সেজন্য স্বয়ংক্রিয়ভাবে সামনে `'` বসানো হয়।
