# Net Express — ডিপ্লয়মেন্ট গাইড (সহজ বাংলায়)

এই গাইডে তিনটা আলাদা অংশ আছে — **Vercel**, **Railway**, আর ভবিষ্যতের জন্য **Hostinger VPS**। যেদিন যেখানে সাইট চলছে, শুধু সেই অংশটাই পড়লে চলবে — বাকি অংশ পড়ার দরকার নেই।

---

## ১. Vercel

এই মুহূর্তে সাইটটা এই পদ্ধতিতেই চলছে ধরে নেওয়া হচ্ছে: GitHub রিপোজিটরি ↔ Vercel সরাসরি যুক্ত (connected)।

### `git push` করলে কী হয়

GitHub-এর `main` ব্রাঞ্চে যখনই নতুন কোড push করা হয়, Vercel সেটা স্বয়ংক্রিয়ভাবে ধরে ফেলে এবং নিজে নিজে একটা নতুন বিল্ড শুরু করে (এটাকে বলে "auto-deploy")। কোনো বাটনে চাপ দেওয়ার দরকার নেই — এটা এমনিতেই ২-৩ মিনিটের মধ্যে শেষ হয়ে যায়।

### লাইভ ওয়েবসাইটের লিংক কোথায় পাবেন

Vercel ড্যাশবোর্ডে (vercel.com) লগইন করে প্রজেক্টে ঢুকুন। প্রজেক্টের উপরের দিকে "Visit" বা ডোমেইন নামের একটা লিংক দেখা যাবে — এটাই লাইভ সাইটের ঠিকানা। যদি একটা কাস্টম ডোমেইন (যেমন netexpress.com.bd) যুক্ত করা থাকে, সেটাও এখানে "Domains" ট্যাবে দেখা যাবে।

### ডিপ্লয় ফেইল হয়েছে কিনা কীভাবে বুঝবেন

1. Vercel ড্যাশবোর্ডে প্রজেক্টে ঢুকুন।
2. "Deployments" ট্যাবে সব ডিপ্লয়মেন্টের একটা তালিকা দেখা যাবে, প্রতিটার পাশে একটা স্ট্যাটাস চিহ্ন থাকবে:
   - সবুজ "Ready" = সফল, লাইভ হয়ে গেছে।
   - লাল "Error" / "Failed" = ব্যর্থ হয়েছে, লাইভ সাইটে কোনো পরিবর্তন যায়নি (আগের ভালো ভার্সনটাই লাইভ আছে)।
3. ব্যর্থ ডিপ্লয়মেন্টে ক্লিক করে "Build Logs" দেখলে লাল রঙে এরর মেসেজ পাওয়া যাবে — সেটা কোন ফাইলে সমস্যা তা বলে দেয় (ডেভেলপারকে স্ক্রিনশট পাঠালেই যথেষ্ট)।

### আগের ভার্সনে ফিরে যাওয়া (Rollback)

যদি নতুন ডিপ্লয়মেন্টে কোনো সমস্যা দেখা যায় (এমনকি বিল্ড সফল হলেও, যদি ভুল কনটেন্ট লাইভ হয়ে যায়):

1. "Deployments" ট্যাবে যান।
2. যেই আগের ডিপ্লয়মেন্টটা ঠিকঠাক কাজ করছিল, সেটা খুঁজে বের করুন (তারিখ ও সময় দেখে চেনা যাবে)।
3. সেই ডিপ্লয়মেন্টের পাশে থাকা তিন-ডট (`...`) মেনুতে ক্লিক করুন।
4. **"Promote to Production"** (বা কোনো Vercel ভার্সনে "Redeploy") অপশনে ক্লিক করুন।
5. কনফার্ম করলেই লাইভ সাইট তৎক্ষণাৎ সেই আগের ভার্সনে ফিরে যাবে — নতুন করে বিল্ড করারও দরকার নেই।

### Environment Variables (.env) কোথায় বসাবেন

লোকাল কম্পিউটারে `.env` ফাইলে যেসব ভ্যারিয়েবল থাকে (যেমন `VITE_PAYMENT_URL`), সেগুলো Vercel-এ আলাদাভাবে বসাতে হয় — `.env` ফাইলটা কখনো GitHub-এ আপলোড হয় না বা যায় না, তাই Vercel এটা নিজে থেকে দেখতে পায় না।

1. Vercel ড্যাশবোর্ডে প্রজেক্টে ঢুকে **Settings → Environment Variables** এ যান।
2. প্রতিটা ভ্যারিয়েবলের নাম (যেমন `VITE_PAYMENT_URL`) ও মান আলাদা করে যোগ করুন।
3. সেভ করার পর, পরিবর্তনটা কার্যকর হতে একটা নতুন ডিপ্লয়মেন্ট লাগবে (Deployments ট্যাব থেকে সর্বশেষটা "Redeploy" করলেই হবে, নতুন কোড push করার দরকার নেই)।

---

## ২. Railway

যদি ভবিষ্যতে সাইটটা Railway-তে সরানো হয়, ধাপগুলো প্রায় Vercel-এর মতোই, শুধু জায়গার নাম আলাদা।

### `git push` করলে কী হয়

Railway-ও GitHub-এর সাথে যুক্ত থাকলে `main` ব্রাঞ্চে push করার সাথে সাথেই স্বয়ংক্রিয়ভাবে নতুন বিল্ড শুরু করে দেয় — Vercel-এর মতোই।

### লাইভ লিংক কোথায় পাবেন

railway.app-এ লগইন করে প্রজেক্টে ঢুকুন। সার্ভিসের উপরে "Settings" ট্যাবের "Domains" অংশে (অথবা সরাসরি প্রজেক্ট ওভারভিউ পেজে) লাইভ URL দেখা যাবে।

### ডিপ্লয় ফেইল হয়েছে কিনা কীভাবে বুঝবেন

1. প্রজেক্টে ঢুকে "Deployments" ট্যাবে যান।
2. প্রতিটা ডিপ্লয়মেন্টের পাশে স্ট্যাটাস দেখা যাবে — সবুজ "Success"/"Active" মানে ঠিক আছে, লাল "Failed"/"Crashed" মানে সমস্যা হয়েছে।
3. ব্যর্থ ডিপ্লয়মেন্টে ক্লিক করলে বিল্ড ও ডিপ্লয় লগ দেখা যায়, যেখানে লাল রঙে এরর মেসেজ থাকবে।

### আগের ভার্সনে ফিরে যাওয়া (Rollback)

1. "Deployments" ট্যাবে গিয়ে আগের সফল ডিপ্লয়মেন্টটা খুঁজুন।
2. তার পাশের তিন-ডট মেনু থেকে **"Redeploy"** অপশনে ক্লিক করুন — এতে সেই পুরনো ভার্সনটাই আবার লাইভ করা হবে।

### Environment Variables কোথায় বসাবেন

প্রজেক্টের সার্ভিসে ঢুকে **"Variables"** ট্যাবে যান। এখানে `VITE_PAYMENT_URL`, `VITE_SITE_URL` ইত্যাদি প্রতিটা ভ্যারিয়েবল নাম ও মান দিয়ে যোগ করুন। সেভ করলে Railway নিজে থেকেই একটা নতুন ডিপ্লয়মেন্ট শুরু করে দেয় যাতে পরিবর্তনটা কার্যকর হয়।

> এই প্রজেক্টে ইতিমধ্যে `railway.json` ও `nixpacks.toml` ফাইল আছে, যেগুলো Railway-কে বলে দেয় কীভাবে বিল্ড ও স্টার্ট করতে হবে (`npm run build` দিয়ে বিল্ড, `npm start` দিয়ে চালু) — এগুলোতে হাত দেওয়ার দরকার নেই।

---

## ৩. Hostinger VPS (ভবিষ্যতের জন্য)

এটা তুলনামূলক টেকনিক্যাল একটা পদ্ধতি — সরাসরি নিজের সার্ভার ব্যবহার করা, Vercel/Railway-এর মতো কোনো "অটো-ডিপ্লয়" সুবিধা এখানে বাই-ডিফল্ট থাকে না। ধাপগুলো নিচে সহজভাবে দেওয়া হলো।

### ধাপ ১ — নিজের কম্পিউটারে বিল্ড করা

টার্মিনাল/কমান্ড প্রম্পট খুলে প্রজেক্ট ফোল্ডারে গিয়ে লিখুন:

```bash
npm run build
```

এটা চালালে ওয়েবসাইটের সব লেখা, ছবি, কোড একত্র করে **অপটিমাইজড, দ্রুতগতির** স্ট্যাটিক ফাইল তৈরি হয়।

### `dist/` ফোল্ডার কী

উপরের কমান্ডটা শেষ হলে একটা নতুন ফোল্ডার তৈরি হবে — `dist/`। এর ভেতরে HTML, CSS, JavaScript, ছবি — সব রেডিমেড ফাইল থাকবে যা সরাসরি একটা ব্রাউজারে খোলা/দেখানো যায়। **এই `dist/` ফোল্ডারটাই** আসলে সার্ভারে আপলোড করতে হবে — বাকি প্রজেক্ট ফোল্ডার (যেমন `src/`, `node_modules/`) সার্ভারে দেওয়ার দরকার নেই।

### ধাপ ২ — VPS-এ আপলোড করা

`dist/` ফোল্ডারের সব ফাইল VPS-এ আপলোড করুন। এটা করার সহজ কিছু উপায়:

- **FileZilla** (SFTP ক্লায়েন্ট) দিয়ে drag-and-drop করে আপলোড করা।
- অথবা টার্মিনাল থেকে `scp -r dist/* user@your-vps-ip:/var/www/netexpress/` এর মতো একটা কমান্ড দিয়ে।

সাধারণত ফাইলগুলো `/var/www/netexpress/` (বা এই ধরনের একটা ফোল্ডার) এ রাখা হয়।

### ধাপ ৩ — Nginx দিয়ে সার্ভ করা

এই ওয়েবসাইটটা একটা "SPA" (Single Page Application) — মানে React Router দিয়ে পেজ পরিবর্তন হয়, প্রতিবার নতুন করে সার্ভারে রিকোয়েস্ট যায় না। এই কারণে **একটা বিশেষ নিয়ম** ছাড়া Nginx কনফিগার করলে, কেউ যদি `/packages` বা `/offers` পেজে থেকে ব্রাউজার রিফ্রেশ (F5) করে, তাহলে **404 এরর** দেখাবে।

এটা এড়াতে Nginx কনফিগ ফাইলে (সাধারণত `/etc/nginx/sites-available/netexpress`) এই রকম একটা বেসিক সেটআপ ব্যবহার করতে হবে:

```nginx
server {
    listen 80;
    server_name netexpress.com.bd;
    root /var/www/netexpress;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

**সবচেয়ে গুরুত্বপূর্ণ লাইনটা হলো:** `try_files $uri $uri/ /index.html;` — এটাই বলে দেয় যে কোনো পেজ খুঁজে না পেলে (যেমন `/packages` রিফ্রেশ করলে), Nginx যেন 404 না দেখিয়ে `index.html` ফেরত দেয়, আর তারপর React Router নিজে বুঝে নেয় কোন পেজ দেখাতে হবে। এই লাইনটা ছাড়া রিফ্রেশ করলেই সাইট ভেঙে যাবে (404)।

কনফিগ বসানোর পর:

```bash
sudo ln -s /etc/nginx/sites-available/netexpress /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

### Environment Variables (সার্ভার সাইডে) কোথায় বসাবেন

এখানে একটা গুরুত্বপূর্ণ পার্থক্য আছে: এই প্রজেক্টের `VITE_...` ভ্যারিয়েবলগুলো (`VITE_PAYMENT_URL` ইত্যাদি) **বিল্ড করার সময়** ফাইলের ভেতরে বসে যায় — মানে এগুলো `npm run build` চালানোর *আগে*, লোকাল কম্পিউটারে (অথবা যেখানে বিল্ড হচ্ছে সেখানে) `.env` ফাইলে বসাতে হবে, VPS-এ বিল্ড-পরবর্তী কোনো সেটিংসে নয়। তাই:

1. যেই মেশিনে `npm run build` চালাবেন, সেখানে `.env.example` কপি করে `.env` বানান এবং আসল মান বসান।
2. তারপর বিল্ড করুন — তখনই এই মানগুলো `dist/` ফোল্ডারের ফাইলে স্থায়ীভাবে বসে যাবে।
3. VPS শুধু এই রেডিমেড `dist/` ফোল্ডার সার্ভ করে — VPS-কে আলাদা করে কোনো এনভায়রনমেন্ট ভ্যারিয়েবল জানানোর দরকার নেই (যদি না ভবিষ্যতে সত্যিকারের ব্যাকএন্ড সার্ভার যোগ করা হয়, তখন সেই সার্ভার প্রসেসের জন্য VPS-এ আলাদাভাবে `.env` বা systemd এনভায়রনমেন্ট ফাইল লাগবে)।

---

আরও দেখুন: ওয়েবসাইটের লেখা, দাম, ছবি নিজে পরিবর্তন করার জন্য **[MAINTENANCE-GUIDE.md](./MAINTENANCE-GUIDE.md)** পড়ুন।
