# Net Express — মেইনটেন্যান্স গাইড (সহজ বাংলায়)

এই গাইডটি লেখা হয়েছে তাদের জন্য যারা কোডিং জানেন না, কিন্তু ওয়েবসাইটের লেখা, দাম, ছবি বা লিংক নিজে থেকে পরিবর্তন করতে চান।

> **শুরুর আগে একটা কথা:** এই ওয়েবসাইটটা এমনভাবে তৈরি — শুধুমাত্র নির্দিষ্ট কিছু ফাইল এডিট করলেই ওয়েবসাইটের লেখা/দাম/ছবি বদলে যায়। আপনাকে কখনো `src/components/`, `src/sections/`, বা `src/pages/` ফোল্ডারের ফাইল ছুঁতে হবে না — শুধু নিচের টেবিলে দেখানো ফাইলগুলোই যথেষ্ট।

---

## ক. কী পরিবর্তন করতে চাইলে কোথায় যাবেন

| কী পরিবর্তন করতে চান | কোন ফাইলে যাবেন |
|---|---|
| কোম্পানির নাম, ফোন নম্বর, ইমেইল, ঠিকানা, BTRC লাইসেন্স নম্বর, হটলাইন, অফিস টাইম | `src/config/company.js` |
| বিল পরিশোধের লিংক ও পেমেন্ট মেথড (বিকাশ/নগদ/রকেট/কার্ড) | `src/config/payment.js` (অথবা `.env` ফাইলে `VITE_PAYMENT_URL`, দেখুন নিচে) |
| ফেসবুক, ইনস্টাগ্রাম, ইউটিউব, হোয়াটসঅ্যাপ, মেসেঞ্জার লিংক | `src/config/social.js` |
| হোম প্যাকেজ ও গেমিং প্যাকেজ (দাম, স্পিড, ফিচার) | `src/data/packages.js` |
| SME ও কর্পোরেট প্ল্যান | `src/data/smePlans.js` |
| অফার/প্রোমোশনের লেখা | `src/data/offers.js` |
| অফারের ছবি | `src/assets/images/offers/` (যেমন `offer-01.svg`) |
| সচরাচর জিজ্ঞাসা (FAQ) | `src/data/faq.js` |
| হোমপেজের পরিসংখ্যান (গ্রাহক সংখ্যা, আপটাইম, অভিজ্ঞতা ইত্যাদি) | `src/data/statistics.js` |
| উপরের মেনু ও ফুটারের লিংকের নাম | `src/data/navigation.js` |
| হোমপেজের বড় হিরো ছবি (উপরের দিকের ছবি) | `src/assets/images/home/hero.svg` |
| লোগো (নেভবার ও ফুটার) | `src/assets/logo/logo.svg` এবং `src/assets/logo/logo-white.svg` |
| গ্রাহকদের মতামত/রিভিউ * | `src/data/testimonials.js` |
| শর্তাবলী (Terms) ও গোপনীয়তা নীতি (Privacy Policy) পেজের লেখা | `src/i18n/translations.js` ফাইলের ভেতরে `terms` ও `privacy` অংশ |
| ফুটারের কুইক লিংক ও সাপোর্ট লিংক | `src/data/navigation.js` (নিচের দিকে `footerLinks`) |
| এলাকাভিত্তিক কভারেজ চেকার তালিকা | `src/data/coverageAreas.js` |
| রং/থিম | `src/styles/variables.css` (দেখুন সেকশন গ — নিচে) |

\* বর্তমানে গ্রাহকদের মতামত (testimonials) ফাইলটি প্রস্তুত করা আছে, কিন্তু কোনো পেজে এখনো সংযুক্ত (connected) করা হয়নি — অর্থাৎ এই মুহূর্তে এই ফাইল এডিট করলে ওয়েবসাইটে সাথে সাথে কোনো পরিবর্তন দেখা যাবে না। এটা কোনো পেজে দেখাতে হলে একজন ডেভেলপারের সাহায্য লাগবে।

**সবসময় মনে রাখবেন:** যেকোনো ফাইলে `bn` মানে বাংলা লেখা, আর `en` মানে ইংরেজি লেখা। শুধু `bn: "..."` এর ভেতরের লেখাটা পরিবর্তন করলেই বাংলা সাইটে পরিবর্তন দেখা যাবে। `en: "..."` ও চাইলে বদলাতে পারেন (ইংরেজি ভার্সনের জন্য), কিন্তু জরুরি না হলে বাদ দিতে পারেন।

---

## খ. নতুন প্যাকেজ/অফার/FAQ যোগ করবেন কীভাবে

সাধারণ নিয়ম: প্রতিটা ফাইলে একগুচ্ছ `{ ... }` ব্লক আছে, একটার পর একটা কমা (`,`) দিয়ে আলাদা করা। নতুন কিছু যোগ করতে হলে —

1. একটা পুরনো ব্লক (একদম `{` থেকে তার বন্ধ `}` পর্যন্ত, শেষে একটা কমাসহ) সম্পূর্ণ কপি করুন।
2. সেটা ওই তালিকার শেষে (শেষ `]` চিহ্নের ঠিক আগে) পেস্ট করুন।
3. শুধু `"..."` কোটেশন চিহ্নের ভেতরের লেখা বদলান — বাইরের `{`, `}`, `:`, `,` চিহ্নগুলো একদম অক্ষত/অপরিবর্তিত রাখুন।
4. ফাইল সেভ করুন। কাজ শেষ — নতুন প্যাকেজ/অফার/FAQ স্বয়ংক্রিয়ভাবে ওয়েবসাইটে দেখা যাবে।

### নতুন প্যাকেজ যোগ করা — `src/data/packages.js`

`packages` তালিকার শেষে (শেষ `];` এর আগে) এভাবে একটা ব্লক পেস্ট করুন এবং লেখা বদলান:

```javascript
{
  id: "home-99",              // এই আইডি অবশ্যই অন্য প্যাকেজের চেয়ে আলাদা হতে হবে
  category: "home",           // "home", "gaming", অথবা "business" — এই তিনটার একটাই লিখুন
  tag: null,                  // চাইলে { bn: "নতুন", en: "New" } এর মতো ছোট ব্যাজ দিতে পারেন, নাহলে null রাখুন
  name: { bn: "নতুন প্যাকেজ", en: "New Package" },
  speed: { bn: "৯৯ Mbps", en: "99 Mbps" },
  speedValue: 99,
  price: { bn: "১২০০", en: "1200" },
  period: { bn: "মাস", en: "month" },
  vatNote: { bn: "(৫% ভ্যাটসহ)", en: "(VAT 5% included)" },
  whatsNew: null,              // চাইলে { bn: "...", en: "..." } দিয়ে একটা হাইলাইট লেখা দেখাতে পারেন
  features: shortFeatures,     // চারটি স্ট্যান্ডার্ড ফিচার লাইনের জন্য এভাবেই রাখুন
  popular: false,              // true করলে "সবচেয়ে জনপ্রিয়" ব্যাজ দেখাবে
},
```

### নতুন অফার যোগ করা — `src/data/offers.js`

প্রথমে অফারের ছবিটা `src/assets/images/offers/` ফোল্ডারে রাখুন (যেমন `offer-04.svg`)। তারপর ফাইলের একদম উপরে এই লাইনটা যোগ করুন (অন্য `import offer0...` লাইনগুলোর নিচে):

```javascript
import offer04 from "../assets/images/offers/offer-04.svg";
```

তারপর `offers` তালিকার শেষে এই ব্লকটা পেস্ট করে লেখা বদলান:

```javascript
{
  id: "offer-04",
  image: offer04,             // উপরে যে নামে import করলেন, ঠিক সেই নামটা এখানে বসবে
  badge: { bn: "বিশেষ অফার", en: "Special Offer" },
  title: { bn: "আপনার অফারের শিরোনাম", en: "Your Offer Title" },
  description: {
    bn: "অফারের বিস্তারিত লেখা এখানে দিন।",
    en: "Offer details go here.",
  },
  validity: { bn: "৩১ ডিসেম্বর পর্যন্ত", en: "Until December 31" },
},
```

### নতুন FAQ যোগ করা — `src/data/faq.js`

`faqs` তালিকার শেষে এই ব্লকটা পেস্ট করে লেখা বদলান:

```javascript
{
  id: "faq-7",                 // পরের সংখ্যা দিন, যেমন আগের শেষটা faq-6 হলে এটা faq-7
  question: { bn: "আপনার প্রশ্নটি এখানে লিখুন?", en: "Your question here?" },
  answer: {
    bn: "প্রশ্নের উত্তর এখানে লিখুন।",
    en: "The answer goes here.",
  },
},
```

---

## গ. রং/ফন্ট পরিবর্তন

সাইটের রং নিয়ন্ত্রণ হয় **`src/styles/variables.css`** ফাইল দিয়ে। এখানে `--primary-red` নামের একটা মান আছে — এটাই সাইটের প্রধান লাল রং, যা বাটন, দাম, হাইলাইট, ব্যাজ ইত্যাদি সব জায়গায় ব্যবহার হয়। এই একটা মান বদলালে পুরো সাইটের লাল রং একসাথে বদলে যাবে (কোথাও আলাদা করে রং হার্ডকোড করা নেই)।

ফন্ট এই একই স্টাইল সিস্টেমে সংজ্ঞায়িত, কিন্তু ফন্ট বদলানো তুলনামূলক ঝুঁকিপূর্ণ (Google Fonts থেকে লোড হয়) — তাই ফন্ট বদলাতে চাইলে একজন ডেভেলপারের সাহায্য নেওয়াই ভালো।

---

## ঘ. সতর্কতা — যা করবেন না

ফাইলগুলো JavaScript কোড, তাই কিছু নিয়ম মানতেই হবে, নাহলে পুরো ওয়েবসাইট ভেঙে যেতে পারে:

- **কমা (`,`) মুছে ফেলবেন না।** প্রতিটা `{ ... }` ব্লকের পর একটা কমা থাকে (তালিকার একদম শেষ ব্লক ছাড়া)। কমা ভুলে মুছে গেলে পুরো ফাইল ভেঙে যাবে।
- **বন্ধনী/ব্র্যাকেট (`{`, `}`, `[`, `]`) মুছে ফেলবেন না।** প্রতিটা খোলা বন্ধনীর একটা বন্ধ বন্ধনী থাকতেই হবে। ব্লক কপি করার সময় পুরো ব্লকটা (শুরুর `{` থেকে শেষের `}` পর্যন্ত) একসাথে কপি করুন, আংশিক নয়।
- **`id` ফিল্ডের নাম পরিবর্তন করবেন না** (যেমন `"genz-35"`, `"faq-1"`)। এই আইডিগুলো কোডের অন্য জায়গায় রেফারেন্স হিসেবে ব্যবহার হতে পারে — নাম বদলালে বা মুছে দিলে ওই অংশ কাজ করা বন্ধ করে দিতে পারে। নতুন এন্ট্রি যোগ করার সময় শুধু একটা **নতুন, অনন্য** আইডি দিন, পুরনোটা বদলাবেন না।
- **`"..."` কোটেশনের বাইরের কিছু বদলাবেন না** — শুধু কোটেশনের ভেতরের লেখা বদলান। `bn:`, `en:`, `price:` এই ধরনের শব্দগুলো (যাকে বলে "কী" — key) অক্ষত রাখুন, নাহলে সাইট আর ডেটা খুঁজে পাবে না।

### বিল্ড ভেঙে গেলে কীভাবে বুঝবেন

- **ডেভেলপমেন্টে (নিজের কম্পিউটারে) কাজ করার সময়:** ব্রাউজারে সাদা/ফাঁকা পেজ দেখাবে, অথবা স্ক্রিনে লাল রঙের এরর মেসেজ দেখাবে।
- **Vercel-এ ডিপ্লয় করার সময়:** Vercel-এর ড্যাশবোর্ডে সেই ডিপ্লয়মেন্টের পাশে লাল "Failed" চিহ্ন দেখাবে, এবং লাইভ সাইটে নতুন পরিবর্তন প্রকাশ পাবে না (আগের ভালো ভার্সনটাই লাইভ থেকে যাবে, তাই ভয়ের কিছু নেই)।
- **সমাধান:** যে ফাইলটা শেষ এডিট করেছেন, সেটাতে ফিরে গিয়ে চেক করুন — কোনো কমা, বন্ধনী বা কোটেশন চিহ্ন কি ভুলবশত মুছে গেছে বা বাড়তি বসে গেছে? সহজ উপায় হলো, সমস্যা হলে সেই ফাইলের এডিট বাতিল করে (git-এ আগের ভার্সনে ফিরে গিয়ে) আবার সাবধানে চেষ্টা করা। নিশ্চিত না হলে একজন ডেভেলপারকে ফাইলটা দেখিয়ে সাহায্য চান।

---

## ঙ. সংযোগের অনুরোধ ফর্ম কোথায় জমা হয় (Google Sheet)

"নতুন সংযোগের জন্য আবেদন করুন" ফর্মে কেউ তথ্য জমা দিলে সেটা সরাসরি একটা **Google Sheet**-এ নতুন সারি হিসেবে যোগ হয় (কোনো ওয়েবসাইট রিডাইরেক্ট বা নতুন ট্যাব ছাড়াই, ইউজার পেজেই থেকে যান)। এটা কাজ করে **`VITE_SHEETS_WEBHOOK_URL`** নামের একটা ঠিকানার (Google Apps Script "web app" URL) মাধ্যমে।

- জমা হওয়া তথ্য দেখতে: যে Google Sheet-টা Apps Script-এর সাথে যুক্ত করা আছে, সেটা Google Drive-এ খুলুন।
- ঠিকানা বদলাতে (যেমন নতুন Sheet বা নতুন Apps Script deploy করলে): `.env` ফাইলে (লোকাল কম্পিউটারে) এবং Vercel-এর Environment Variables-এ (লাইভ সাইটের জন্য) `VITE_SHEETS_WEBHOOK_URL`-এর মান নতুন URL দিয়ে বদলে দিন — দুই জায়গাতেই বদলাতে হবে, নাহলে লাইভ সাইট পুরনো ঠিকানায় ডেটা পাঠাতে থাকবে।
- এই ঠিকানা খালি থাকলে বা ভুল হলে, ফর্ম সাবমিট করার সময় ইউজার একটা এরর মেসেজ দেখবেন (হটলাইন নম্বর সহ) — সাইট ভাঙবে না।

---

আরও দেখুন: ওয়েবসাইট কীভাবে লাইভ হয় ও কীভাবে আপলোড/আপডেট করতে হয় তার জন্য **[DEPLOYMENT-GUIDE.md](./DEPLOYMENT-GUIDE.md)** পড়ুন।
